[pytest]
minversion = 7.0
testpaths = tests/active
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q
markers =
    active: current release-gate tests that are expected to pass by default
    legacy: historical tests retained for cleanup inventory; not collected by default
