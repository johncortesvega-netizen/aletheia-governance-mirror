diff -uNr -x __pycache__ -x .pytest_cache patch13_base/PATCH_13_RECOVERY_NOTE.md patch13_mod/PATCH_13_RECOVERY_NOTE.md
--- patch13_base/PATCH_13_RECOVERY_NOTE.md	1970-01-01 00:00:00.000000000 +0000
+++ patch13_mod/PATCH_13_RECOVERY_NOTE.md	2026-05-09 07:19:47.972758497 +0000
@@ -0,0 +1,58 @@
+# ALETHEIA PATCH RECOVERY NOTE
+
+Patch: 13
+Name: Code Hygiene + Language Consistency
+Type: Cleanup / low-risk refactor
+
+## Intent
+
+Keep the working Patch 12 build clean before adding new intelligence layers.
+This patch removes minor technical debt and aligns repair-loop text with the
+English app voice introduced in Patch 10.
+
+## Touched files
+
+- `app.py`
+- `protocol.py`
+- `core/parser.py`
+- `requirements.txt`
+- `requirements-llm.txt`
+- `tests/test_protocol_repair_loop.py`
+- `tests/test_patch_13_hygiene_contract.py`
+- `PATCH_13_RECOVERY_NOTE.md`
+
+## What changed
+
+- Removed duplicate `import hashlib` from `app.py`.
+- Converted `protocol_repair_questions(...)` from Dutch to English.
+- Made OpenAI optional in `core/parser.py`; local scan remains the fallback.
+- Moved `openai>=1.30` into `requirements-llm.txt` so local/core builds stay lighter.
+- Added tests for cleanup and language consistency.
+
+## Not touched
+
+- No scoring formulas changed.
+- No verdict logic changed.
+- No capture-marker logic changed.
+- No witness hashing changed.
+- No UI layout changed.
+- No empirical validation claims added.
+- No Global ID sync, public ledger, or push-warning behavior added.
+
+## Recovery
+
+If this patch causes trouble:
+
+1. Restore the duplicate-safe import section from Patch 12 `app.py` if needed.
+2. Restore the previous `protocol_repair_questions(...)` text block if Dutch output was intentional.
+3. Move `openai>=1.30` back into `requirements.txt` if the deployment expects AI Deep Scan as a hard dependency.
+4. Remove `requirements-llm.txt` and `tests/test_patch_13_hygiene_contract.py`.
+
+## Validation
+
+Run:
+
+```bash
+python -m py_compile app.py protocol.py core/scoring.py core/parser.py core/witness.py
+python -m pytest tests -q
+```
diff -uNr -x __pycache__ -x .pytest_cache patch13_base/app.py patch13_mod/app.py
--- patch13_base/app.py	2026-05-09 06:41:22.309030651 +0000
+++ patch13_mod/app.py	2026-05-09 07:19:47.971524456 +0000
@@ -2,7 +2,6 @@
 import json
 import zipfile
 import hashlib
-import hashlib
 import random
 import re
 import io
diff -uNr -x __pycache__ -x .pytest_cache patch13_base/core/parser.py patch13_mod/core/parser.py
--- patch13_base/core/parser.py	2026-05-09 05:53:16.426671298 +0000
+++ patch13_mod/core/parser.py	2026-05-09 07:19:47.972428770 +0000
@@ -2,7 +2,10 @@
 import json
 import re
 import streamlit as st
-from openai import OpenAI
+try:
+    from openai import OpenAI
+except Exception:
+    OpenAI = None
 
 
 # Patch 07 — Invisibility Filter / actor decoupling
@@ -263,7 +266,7 @@
     except Exception:
         api_key = os.getenv("OPENAI_API_KEY")
 
-    if not api_key:
+    if not api_key or OpenAI is None:
         return _local_governance_scan(query)
 
     client = OpenAI(api_key=api_key)
diff -uNr -x __pycache__ -x .pytest_cache patch13_base/protocol.py patch13_mod/protocol.py
--- patch13_base/protocol.py	2026-05-09 05:48:00.793324649 +0000
+++ patch13_mod/protocol.py	2026-05-09 07:19:47.972114389 +0000
@@ -194,25 +194,25 @@
     risk = (corruption_risk or "").lower()
 
     questions: list[str] = [
-        "Welke bezwaarroute bestaat voor mensen die door dit voorstel geraakt worden?",
-        "Wie kan de uitvoerende macht corrigeren, pauzeren of herzien?",
-        "Welke auditsporen maken misbruik zichtbaar zonder ALETHEIA zelf beslissingsmacht te geven?",
+        "What appeal path exists for people affected by this proposal?",
+        "Who can pause, correct, or review the executive power in this design?",
+        "Which audit trail makes misuse visible without giving ALETHEIA decision power?",
     ]
 
     if v == "ASYLUM":
-        questions.insert(0, "Welke drie checks-and-balances zouden dit patroon richting herstel kunnen bewegen?")
-        questions.append("Welke macht moet tijdelijk, toetsbaar of herroepbaar worden gemaakt voordat menselijke review verdergaat?")
+        questions.insert(0, "Which three checks and balances could move this pattern toward repair?")
+        questions.append("Which power should become temporary, reviewable, or revocable before human review continues?")
     elif v == "THRESHOLD":
-        questions.insert(0, "Welke ontbrekende safeguard houdt dit voorstel nog buiten Sanctuary?")
-        questions.append("Welke transparantie-, appeal- of oversight-laag zou eerst expliciet moeten worden gemaakt?")
+        questions.insert(0, "Which missing safeguard is keeping this proposal outside Sanctuary?")
+        questions.append("Which transparency, appeal, or oversight layer should be made explicit first?")
     else:
-        questions.append("Welke waarborg voorkomt dat deze gezonde toestand later alsnog in ownership of capture kantelt?")
+        questions.append("Which safeguard keeps this healthy state from drifting into ownership or capture later?")
 
     if any(term in label for term in ["throne", "capture", "sovereignty", "dictatorship", "surveillance", "global id"]):
-        questions.append("Wat voorkomt dat stewardship hier verandert in ownership, surveillance of een Throne?")
+        questions.append("What keeps stewardship here from becoming ownership, surveillance, or a Throne?")
 
     if risk == "high":
-        questions.append("Welke onafhankelijke partij kan de evidence, scoring en uitzonderingen controleren?")
+        questions.append("Which independent reviewer can check the evidence, scoring, and exceptions?")
 
     # Preserve order while removing duplicates.
     seen: set[str] = set()
diff -uNr -x __pycache__ -x .pytest_cache patch13_base/requirements-llm.txt patch13_mod/requirements-llm.txt
--- patch13_base/requirements-llm.txt	1970-01-01 00:00:00.000000000 +0000
+++ patch13_mod/requirements-llm.txt	2026-05-09 07:19:47.972586408 +0000
@@ -0,0 +1,3 @@
+# Optional LLM parsing dependency.
+# Install this only when AI Deep Scan is needed.
+openai>=1.30
diff -uNr -x __pycache__ -x .pytest_cache patch13_base/requirements.txt patch13_mod/requirements.txt
--- patch13_base/requirements.txt	2026-05-09 04:59:39.000000000 +0000
+++ patch13_mod/requirements.txt	2026-05-09 07:19:47.972540988 +0000
@@ -2,7 +2,6 @@
 numpy>=1.26
 pandas>=2.0
 plotly>=5.20
-openai>=1.30
 pytest
 requests
 openpyxl
diff -uNr -x __pycache__ -x .pytest_cache patch13_base/tests/test_patch_13_hygiene_contract.py patch13_mod/tests/test_patch_13_hygiene_contract.py
--- patch13_base/tests/test_patch_13_hygiene_contract.py	1970-01-01 00:00:00.000000000 +0000
+++ patch13_mod/tests/test_patch_13_hygiene_contract.py	2026-05-09 07:19:47.972719889 +0000
@@ -0,0 +1,38 @@
+"""
+ALETHEIA RECOVERY NOTE
+Patch 13: Code hygiene + language consistency tests.
+
+Purpose:
+    Keep cleanup changes small and reviewable: no duplicate hashlib import,
+    repair-loop wording in English, and OpenAI kept optional for local builds.
+
+Rollback:
+    Remove this test file and revert Patch 13 cleanup changes.
+"""
+
+from pathlib import Path
+
+ROOT = Path(__file__).resolve().parents[1]
+APP_TEXT = (ROOT / "app.py").read_text(encoding="utf-8")
+PARSER_TEXT = (ROOT / "core" / "parser.py").read_text(encoding="utf-8")
+REQ_TEXT = (ROOT / "requirements.txt").read_text(encoding="utf-8")
+LLM_REQ_TEXT = (ROOT / "requirements-llm.txt").read_text(encoding="utf-8")
+PROTOCOL_TEXT = (ROOT / "protocol.py").read_text(encoding="utf-8")
+
+
+def test_hashlib_import_is_not_duplicated():
+    assert APP_TEXT.count("import hashlib") == 1
+
+
+def test_openai_dependency_is_optional_for_local_builds():
+    assert "openai>=1.30" not in REQ_TEXT
+    assert "openai>=1.30" in LLM_REQ_TEXT
+    assert "OpenAI = None" in PARSER_TEXT
+    assert "if not api_key or OpenAI is None" in PARSER_TEXT
+
+
+def test_protocol_repair_questions_are_english_ui_text():
+    assert "What appeal path exists for people affected by this proposal?" in PROTOCOL_TEXT
+    assert "Which three checks and balances could move this pattern toward repair?" in PROTOCOL_TEXT
+    assert "Welke bezwaarroute" not in PROTOCOL_TEXT
+    assert "Welke drie checks-and-balances" not in PROTOCOL_TEXT
diff -uNr -x __pycache__ -x .pytest_cache patch13_base/tests/test_protocol_repair_loop.py patch13_mod/tests/test_protocol_repair_loop.py
--- patch13_base/tests/test_protocol_repair_loop.py	2026-05-09 05:49:03.055943537 +0000
+++ patch13_mod/tests/test_protocol_repair_loop.py	2026-05-09 07:19:47.972678515 +0000
@@ -44,3 +44,5 @@
     assert all(question.strip().endswith("?") for question in questions)
     assert any("ALETHEIA" in question for question in questions)
     assert any("Throne" in question or "ownership" in question or "capture" in question for question in questions)
+    assert any("appeal path" in question.lower() for question in questions)
+    assert not any("Welke" in question or "Wat " in question for question in questions)
