# Patch 268 recovery note

Patch 268 is documentation/test-only.

To revert this patch, remove the Patch 268 docs/tests/manifest files and restore `PATCH_STATUS.md` and `PATCH_NOTES.md` to the Patch 267 versions.

No runtime files are changed by this patch.
