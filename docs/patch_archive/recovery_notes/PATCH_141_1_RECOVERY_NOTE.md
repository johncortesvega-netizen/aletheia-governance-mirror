# Patch 141.1 Recovery Note — Receipt Reader Support Utility Placement Hotfix

If this patch needs to be reverted, restore `app.py` and `tests/test_patch_141_v1_ui_receipt_upload_cleanup.py` from the prior Patch 141 state.

This hotfix only clarifies placement: Receipt Reader appears inside a clearly labeled `Support utilities` section above the footer. It remains outside the main module tab row and remains upload-only. No scoring, routing, receipt schema, receipt generation, signal, AI Integrity, Privacy Audit, World Lens, storage, telemetry, or authority behavior is changed.
