# Receipt Reader Placement Rebase

**Patch:** 240  
**Status:** navigation/copy clarification only  

Receipt Reader is intentionally located under:

`Why ALETHEIA → Support utilities → Receipt Reader — Standard View`

It is not a top-level decision module. It is a read-only support utility for inspecting ALETHEIA receipts, native values, semantic pressure layers, and plain-English explanations without rescoring, certifying, approving, rejecting, enforcing, or overriding the original receipt.

This patch rebases the placement clarification after the Stage 9 Mirror Check page extraction so the copy remains visible in the current app state.
