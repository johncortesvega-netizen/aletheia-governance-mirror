PATCH 71.11 MANIFEST - Mirror Check Stress Label Row Render Fix

Patch ID: 71.11
Patch name: Mirror Check Stress Label Row Render Fix
Owner/User: Johnny Cortes Vega
Project: ALETHEIA v1.0 - Governance Mirror

Purpose:
- Fix the remaining Mirror Check result-card regression where the Stress label row still appeared as literal code.
- Keep Patch 71.10's HTML-rendering path while making the detail rows render as inline HTML.
- Escape dynamic card text before inserting it into `unsafe_allow_html=True` markup.

Files changed:
- app.py
- tests/test_patch_71_11_mirror_check_stress_label_row_rendering.py
- PATCH_71_11_MANIFEST.txt
- PATCH_71_11_RECOVERY_NOTE.md
- PATCH_STATUS.md
- docs/progress_database.md

Behavioral changes:
- The Mirror Check judgment card now builds detail rows as inline `<div>` HTML blocks joined into `detail_rows_html`.
- The card places `detail_rows_html` on a single inline line inside the outer detail container, preventing Streamlit Markdown from treating the final row as code.
- Dynamic card text (`source`, `corruption_risk`, `stress_label`, review-band label) is HTML-escaped before rendering.

Non-goals:
- No receipt schema change.
- No scoring logic change.
- No verdict-routing change.
- No taxonomy expansion.
- No tree visual change.
- No Stress Test logic change.
- No Boundary Cases change.
- No World Lens change.
- No authority-boundary change.
- No public ledger, Global ID sync, central storage, or automated authority behavior.
