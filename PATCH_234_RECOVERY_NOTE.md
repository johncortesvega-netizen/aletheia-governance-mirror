# Patch 234 Recovery Note

If the app fails after this patch, restore `app.py` from Patch 233 and remove `ui/components/module_headers.py`.

This patch is UI-only. It does not change shared protocol-state calculation or any scoring/routing behavior.
